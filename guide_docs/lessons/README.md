# lessons — 측정해서 배운 것, 다시 하면 안 되는 것

모델·게이트·라벨이 바뀌는 이유는 취향이 아니라 **측정**이다.
평가 원문은 [`../source/`](../source/) · 의도된 공식 분기는 [`../DIVERGENCES.md`](../DIVERGENCES.md).
날짜별 작업 로그(감사 추적)는 `CLAUDE.md` 본문.

---

## 한 줄로 읽는 교훈

이 전략의 돈은 **오버나이트 리스크 프리미엄(구조)** 에서 나오지, 마감 총점의 영리함에서 나오지 않는다.
모델의 역할은 수익 창출보다 **하락 레짐에서 진입을 거르는 게이트**에 가깝다.
유일한 검증된 판별 엣지는 **개장전 간밤 미국장 틸트**(특히 KOSPI). 마감 시점에 그걸 쓰면 미래참조다.

## 음성 결과 (라이브에 넣지 말 것 · 같은 각도로 재추격 금지)

같은 가설을 다른 변수명으로 다시 코딩하지 않는다. 다레짐 표본이 쌓이면 **재측정**은 허용.

| 시도 | 결과 | 파일 |
|---|---|---|
| 마감 코어 팩터로 방향 맞춤 | OOS AUC ≈ 0.50 | `exp_calibrate.py` 판별 부분 |
| 레짐·모멘텀 상호작용 | in-sample 후 OOS 증발 | `exp_regime.py` |
| KOSPI 원천 피처 | in-sample 0.59 → OOS 0.50 | `exp_features.py` |
| KOSDAQ vol_tilt | 구 라벨만 이득. 주 라벨 AUC 0.488 | `exp_guarded.py --label open` |
| 미국 지수선물 15:00 배선 | CI 하한 실패 · WF Brier 악화 | `exp_us_futures.py` |
| 간밤 신호로 익일 종가까지 보유 | MDD 급증, 장중 드리프트 음 | `exp_paper.py` S3 |
| news 10% 가중 | 라이브 점수 40/50뿐 · 재구성 불가 | 08-28 상시 `news_na=True` |
| 가중치 그리드 탐색 | train 60% → test 49% 과최적 | `run_backtest --tune` |
| 신뢰도에 신호일치도 곱 | 평상시 0 → 게이트 영구 닫힘 | evaluation6 |
| 사고 토큰 있는 Gemini + 짧은 maxOutput | 본문 0자 · 3-LLM 중 계산 담당 사망 | `gemini_call` |
| BTC 스캘핑 상시 판정(기술 지표 합산) | 60초·5분·15분 전부 45~49% — 방향 못 맞힘 | `exp_btc_scalp.py` |
| BTC 스캘핑 역방향(평균회귀) | 방향 52~55% **재현**되나 판당 총이익이 메이커 비용의 1/8 — 경제성 0 | 〃 반전 스캔(2026-09-01) |
| BTC 이벤트 기반(테이커 불균형·점화·급변) | fade 53~61% 재현·전 조합 메이커 비용 미달. 단기 되돌림은 실재하지만 리테일 비용 아래 | `exp_btc_scalp_events.py` |
| BTC 트리플 배리어 청산 관리(±ATR·스파이크 슬리피지) | 42셀 전원 음수 — ATR 손절이 5분 노이즈에 갈림(SL율 47~66%). '채점이 가치를 숨겼다' 가설 반증. ML/L2 착수 근거 소멸 | `exp_btc_scalp_barrier.py` |
| BTC 프로 도구 2차(VWAP 리버전·CVD 다이버전스·전일고저·시간대·펀딩극단) | VWAP 리버전 54~56% 재현·비용 미달(되돌림 3번째 독립 확인). CVD·레벨 음성. **시간대 후보는 기간 간 비재현**(최근 05시 vs 이전 02/04/09/15시 — 다중검정 데모). 펀딩 극단 120일 0건. 책상 검증 소진 — 남은 미검증은 L2 전방 수집뿐 | `exp_btc_scalp_pro.py` |
| BTC 이벤트→스윙 지평(4h·8h) 연장 | '급변·추종·8h'가 두 독립 120일 창의 4반기 전부 메이커 양수로 생존 — 그러나 240일 풀링 t=0.71, **5년(n=3,009) t=0.11·평균 +0.004%·6년 중 3년 음수 = 통계적 0**. 18콤보 다중검정의 기대 오탐(~1.1개)이 정확히 실현된 사례. 60초~8h 전 지평 종결 — 비용 넘는 진입 트리거는 12h(리포트 트랙) 이상에만 존재 | `exp_btc_swing_events.py` |

## 양성으로 라이브에 남은 것

| 변경 | 조건 | DIVERGENCES |
|---|---|---|
| 적응형 캘리브레이션 | 비관편향 제거. 기울기 하한이면 기저율 표시 | #1 |
| 간밤 고정틸트 | **개장전만**. 학습 재적합은 단일레짐이라 보류 | #10 |
| σ_AM 주 타점 | 전략 지평(하룻밤)과 ATR 카드 정합 | #3 |
| 게이트 우선 사이징 | 위험인데 숏 25% 모순 제거 | #5 |
| 뉴스 시황 분리 후 점수 제외 | 이중계상 제거 + 검증 불가 가중 0 | #7 #8 |
| σ_AM 갭 EWMA(λ=0.94) | 6년 다레짐 walk-forward QLIKE 유의 개선(DM t≈4.7~4.9) — 방향이 아닌 **변동성**은 예측이 된다 | #12 |
| 주 라벨 close→open | 채점이 실거래와 같게 | #11 (측정→결정) |
| confidence = 완전성 | 일치도 이중계상 제거. 임계 유지 | 08-28 |

검증됨(walk-forward)도 **전부 2026 상반기 상승레짐**. 월간 `revalidate.py` 가 생존 여부를 본다.

## 평가 연보

| 차수 | 점수/메시지 | 코드에 남은 것 |
|---|---|---|
| 1 (`evaluation.md`) | 수급 과소·연속일수·환율 방향 | 수집기 정합, 시황/재료 분리 |
| 2 (`evaluation2.md`) | 계보·재현·실행안전이 10점 경로 | 완전성 4분해, 불변 스냅샷, paper 인프라 |
| 3~5 (대화·plan/) | 전략 하나 닫기, 확률 검증성 | 상태머신, 하네스, 캘리브 |
| 6 (`evaluation6.md`) | 63점. 인프라는 있고 예측기는 없다 | 게이트·지평·백업·Gemini·p_up 격하 |

평가 점수를 올리려고 피처를 추가하는 것은 교훈이 아니다. AUC·Brier·paper 순손익이 척도다.

## 라벨 · 확률 읽는 법

- **주:** `overnight_correct` / `primary_*` — 종가 → 익일 시가
- **보조:** close→close / `secondary_*` — 버리지 않음(비교)
- p_up 헤드라인: 정상 캘리브면 「익일 시가 상승 확률」. `slope_at_floor` 면 「상승 기저율(예측 아님)」
- n<40: 화면 숫자 숨김. 적중 100%를 모델 실력으로 읽지 않음

## SoT 와의 관계

`sample/` 은 설계 정본. 하네스로 정당화된 확장만 `DIVERGENCES.md` 에 산다.
표에 없는 산식 차이 → 버그 → SoT 로 되돌린다 (예: foreign_streak 1일 보너스).
철회된 분기는 표에서 지우지 않고 **철회**로 남긴다 (vol_tilt #2).

---

**다음 걸음:** 여기 음성 결과가 아닌 **새 각도**면 → [`../code/measure.md`](../code/measure.md)로 측정. 다레짐 표본이 쌓였으면 재측정은 허용(월간 `revalidate.py`가 관측). 다음 우선순위는 → [`../roadmap/README.md`](../roadmap/README.md).
